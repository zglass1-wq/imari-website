{
  "name": "imari-private-pages",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node ./scripts/emit.mjs",
    "build:one": "astro build && node ./scripts/emit.mjs --only",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^5.6.1"
  }
}
