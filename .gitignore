# macOS Finder metadata
.DS_Store

# Claude Code local state
.claude/

# Deprecated local-only page snapshots
private/DEPR/

# Astro build tooling — only the built private/*.html is committed, not sources' deps/output
astro-src/node_modules/
astro-src/dist/
astro-src/.astro/
